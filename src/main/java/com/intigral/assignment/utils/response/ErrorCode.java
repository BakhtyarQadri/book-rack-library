package com.intigral.assignment.utils.response;

public enum ErrorCode {
    SERVER_ERROR,
    MISSING_NAME, INVALID_NAME,
    MISSING_DESCRIPTION, INVALID_DESCRIPTION,
    MISSING_RACK_ID, INVALID_RACK_ID;
}
